# 足球欧指分析工具 — 完成概览

## 做了什么
从球探体育 titan007 抓取单场比赛的**百家欧指**，过滤清洗后估算**胜/平/负**的市场共识概率，并结合**体彩官方赔率**给出体彩投资方向的相对价值研判。已对示例比赛 **3000426（水户蜀葵 VS 町田泽维亚）** 端到端跑通。

## 关键数据链路（已逆向验证）
- 竞彩赛事列表：`jc.titan007.com/xml/odds_jc.txt`（GBK），每行 `ScheduleID^竞彩胜^竞彩平^竞彩负^...`，首条即「第一场」。
- 单场百家欧指：`1x2d.titan007.com/{match_id}.js`（UTF-8），`game` 数组每行 27 字段，含各公司初盘/即时赔率、去水概率、凯利，以及 `竞彩官*` 体彩行。
- 网站给出的「主胜率/和率/客胜率」已是 `(1/赔率)/Σ(1/赔率)` 的去水概率，可直接聚合。

## 分析方法
1. 市场共识概率 = 各公司去水隐含概率的均值（另给中位数校验）。
2. 体彩隐含概率 = 体彩官*行即时赔率去水。
3. 价值 `edge = 共识概率 − 体彩隐含概率`；`EV = 共识概率 × 体彩赔率 − 1`。
4. 结论：概率最高方向 = 最被看好赛果；edge 最大方向 = 体彩相对最有价值（若 EV 仍为负则仅相对最不差）。

## 示例结论（3000426）
- 共识概率：客胜 50.24% / 平局 28.12% / 主胜 21.64%。
- 体彩赔率：主 3.95 / 平 3.28 / 客 1.75。
- 推荐：概率最高=**客胜**；体彩相对价值最高=**平局**（edge +1.12pp，但三项 EV 仍为负，因体彩抽水约 11%）。

## 交付文件
- `src/config.py` `client.py` `schedule.py` `odds.py` `analysis.py` `cli.py` — 模块化实现
- `tests/test_analysis.py` — 单测（3 项全过，无需联网）
- `README.md` `pyproject.toml`

## 使用
```bash
python -m src.cli analyze 3000426        # 指定比赛
python -m src.cli analyze --url <oddslist链接>
python -m src.cli first                  # 当日第一场
python -m src.cli list                   # 列出当日竞彩
```
依赖（已装于托管 venv）：httpx, parsel, typer。

## 风险与边界
- 结论基于公开赔率的概率/价值量化，**非赛果预测、非投资建议**。
- 体彩抽水高，严格 EV 多为负；仅作方向参考。
- 接口为逆向所得，站点改版可能导致解析失效（已做字段缺失容错）。
