# 篮球胜负分析工具 — 交付总览

在现有足球欧指分析工具基础上，新增了**篮球胜负（moneyline）方向分析**，与足球版对称、共用同一工程。

## 完成内容

| 模块 | 文件 | 作用 |
|------|------|------|
| 入口 | `main.py` | 新增 `nba` 子命令（`nba 705225` / `nba 周六305` / `nba list` / `nba gui`）|
| 赛事列表 | `src/nba_schedule.py` | 解析 `basket_scheduleid.js`，支持「周六301 / 305 / 705225」映射 |
| 赔率抓取 | `src/nba_odds.py` | 抓取解析 `nba.titan007.com/1x2/data1x2/.../{id}.js`（20 字段 2-way）|
| 分析 | `src/nba_analysis.py` | 市场共识概率 + 体彩 edge/EV 研判 + 报告 |
| GUI | `src/gui.py` | 参数化为足球/篮球双模式，`nba gui` 弹窗输入 |

## 用法

```bash
python main.py nba 705225      # 分析指定比赛（纽约自由人 VS 芝加哥天空）
python main.py nba 周六305     # 按场次号定位（= 705225）
python main.py nba list        # 列出当日 15 场篮球赛事
python main.py nba gui         # 弹窗输入
```

## 示例结论（705225，已实测）

- 市场共识：主胜 75.61% / 客胜 24.39%（106 家机构）
- 体彩即时：主胜 1.07 / 客胜 4.08 → 隐含 主 79.22% / 客 20.78%
- 体彩相对价值最高方向：**客胜**（edge +3.61pp）

## 重要限制

⚠️ **让分 / 大小分数据不可达**：titan007 公共端点只暴露篮球「胜负」数据；让分盘、大小分位于其内部服务器（公网不可达）。本次聚焦**胜负**方向（即竞彩篮球核心 2-way 方向，与用户给的示例 `705225` 完全一致）。如需让分分析，请提供可公开访问的让分数据源。

> 结论为基于公开赔率的概率/价值量化，非赛果预测、非投资建议。
